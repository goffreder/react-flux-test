var React = require('react'),

    Logo = React.createClass({
        render : function() {
            return (
                <h1>MyApp</h1>
            );
        }
    });

module.exports = Logo;
